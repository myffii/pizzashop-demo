server:
  port: 8082

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/pizzashop_db
    username: postgres
    password: postgres
  jpa:
    show-sql: true

api:
  product:
    url: http://localhost:8081/api/